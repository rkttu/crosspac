<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="Crosspac.App.Views.CommandLogView">

    <DockPanel Background="{DynamicResource LogBackground}">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Spacing="10"
                    Margin="10,6" VerticalAlignment="Center">
            <TextBlock Text="Command Log" FontWeight="Bold" Foreground="{DynamicResource LogHeaderForeground}"
                       VerticalAlignment="Center" />
            <Button Content="Copy" Command="{Binding CopyCommand}" />
            <Button Content="Save…" Command="{Binding SaveCommand}" />
            <Button Content="Clear" Command="{Binding ClearCommand}" />
            <CheckBox Content="Auto-scroll" IsChecked="{Binding AutoScroll}"
                      Foreground="{DynamicResource LogHeaderForeground}" VerticalAlignment="Center" />
            <TextBlock Text="{Binding Status}" Foreground="{DynamicResource LogSubtleForeground}"
                       VerticalAlignment="Center" />
        </StackPanel>

        <ScrollViewer x:Name="LogScroller"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Entries}" Margin="10,0,10,8">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid ColumnDefinitions="Auto,*" Margin="0,1">
                            <TextBlock Grid.Column="0" Text="{Binding Time}" Foreground="{DynamicResource LogTimeForeground}"
                                       Margin="0,0,10,0"
                                       FontFamily="Cascadia Code,Menlo,Consolas,monospace" />
                            <TextBlock Grid.Column="1" Text="{Binding Text}" TextWrapping="Wrap"
                                       Foreground="{DynamicResource LogTextForeground}"
                                       FontFamily="Cascadia Code,Menlo,Consolas,monospace" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>

</UserControl>
